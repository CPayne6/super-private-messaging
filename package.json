{
  "name": "super-private-messaging",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.26.2",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "codex": "node scripts/codex-container.mjs",
    "test": "pnpm --filter @spm/protocol test"
  },
  "devDependencies": { "typescript": "5.9.3" }
}
